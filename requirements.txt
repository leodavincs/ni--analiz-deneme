# Minimal bağımlılıklar — ağır kütüphane yok
requests>=2.28
PyYAML>=6.0

# Canlı web paneli (web/server.py) — salt-okunur JSON API
Flask>=3.0

# Üretim WSGI sunucusu (Render/Docker) — yerelde gerekmez, zararsız
gunicorn>=21.0
